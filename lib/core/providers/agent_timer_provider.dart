import 'package:flutter/foundation.dart';

/// Provider for agent timer state.
/// Wraps AgentTimerService and exposes active timers to the UI.
class AgentTimerProvider extends ChangeNotifier {
  // Placeholder for timer state management
  // Full implementation will be added in Phase 5
  
  @override
  void notifyListeners() {
    super.notifyListeners();
  }
}